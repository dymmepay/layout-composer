{
  "name": "layout-composer",
  "version": "1.0.1",
  "files": [
    "dist",
    "types.d.ts"
  ],
  "typings": "types.d.ts",
  "main": "dist/index.js",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^4.2.0",
    "vitest": "^0.31.0"
  }
}
